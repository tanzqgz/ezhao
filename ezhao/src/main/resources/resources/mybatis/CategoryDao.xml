<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper  
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"  
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.ezhao.lamps.dao.CategoryDao">
	<resultMap id="User" type="com.ezhao.lamps.entity.Category">
		<result property="id" column="ID"/>
		<result property="categoryName" column="CATEGORY_NAME"/>
		<result property="categoryParent" column="CATEGORY_PARENT"/>
		<result property="createDate" column="CRETE_DATE"/>
	
	</resultMap>
	
	<select id="findByLoginName" parameterType="String" resultType="Category">
		SELECT * FROM user t WHERE t.loginName = #{loginName}
	</select>
	
	<select id="findByLoginnameAndPassword" parameterType="Category" resultMap="Category">
		SELECT * FROM user t WHERE t.loginName = #{loginName} and t.password = #{password}
	</select>
</mapper>  