<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper  
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"  
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.ezhao.lamps.dao.CategoryDao">
	<resultMap id="category" type="com.ezhao.lamps.entity.Category">
		<result property="id" column="ID"/>
		<result property="categoryName" column="CATEGORY_NAME"/>
		<result property="categoryParent" column="CATEGORY_PARENT"/>
		<result property="createDate" column="CRETE_DATE"/>
	
	</resultMap>
	<select id="findCategorysForCN" resultMap="category">
		SELECT * FROM category c WHERE c.version=0 
	</select>
	<select id="findCategorysForEN" resultMap="category">
		SELECT * FROM category c WHERE c.version=1
	</select>
	
</mapper>  